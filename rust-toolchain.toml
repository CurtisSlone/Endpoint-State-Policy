# Rust Toolchain Version Pinning
# Implements: PO.3.2

[toolchain]
channel = "stable"
components = [
    "clippy",
    "rustfmt",
    "rust-src",
    "rust-analyzer",
    "llvm-tools-preview",
]
profile = "default"
