# CTN elements in wrong order
DEF
VAR base_path string `C:\Test`

STATE global_state
    path string = VAR base_path
STATE_END

CRI AND
CTN wrong_order
    STATE local_state    # STATE before TEST - wrong order
        field string = `value`
    STATE_END
    TEST all all        # TEST should come first
CTN_END
CRI_END
DEF_END
