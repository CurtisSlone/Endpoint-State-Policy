# Using reserved keywords as identifiers
DEF
VAR DEF string `invalid`       # ERROR: DEF is reserved
VAR AND int 100               # ERROR: AND is reserved
VAR STATE string `invalid`    # ERROR: STATE is reserved

STATE CRI                     # ERROR: CRI is reserved
    field string = `value`
STATE_END
DEF_END
